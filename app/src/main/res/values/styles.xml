<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->

        <!-- colorPrimary is used for the default action bar background -->
        <item name="colorPrimary">@color/off_white</item>

        <!-- colorPrimaryDark is used for the status bar -->
        <item name="colorPrimaryDark">#aeaeaf</item>

        <!-- colorAccent is used as the default value for colorControlActivated
             which is used to tint widgets -->
        <item name="colorAccent">@color/green</item>

    </style>


    <style name="AppLoginTheme" parent="Theme.AccountKit">
        <item name="windowNoTitle">false</item>

    </style>
    <style name="FacebookLoginButton">
        <item name="android:typeface">normal</item>
        <item name="android:textColor">@color/fb_blue</item>
        <item name="android:textSize">16sp</item>
        <item name="android:layout_marginTop">10dp</item>
        <item name="android:layout_marginBottom">10dp</item>
        <item name="android:layout_gravity">center_horizontal</item>
    </style>


    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="MenuText">
        <item name="android:textSize">20sp</item>
        <item name="android:textColor">@color/tabTextUnselectedColor</item>
        <item name="android:padding">14dp</item>
        <item name="android:layout_marginLeft">14dp</item>
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:onClick">onClick</item>
        <item name="android:drawablePadding">10dp</item>
    </style>

    <style name="AppThemeC" parent="Theme.AppCompat.Light.NoActionBar">
    </style>

    <style name="AppThemeA" parent="android:Theme.Holo.Light.Dialog.NoActionBar">

        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:backgroundDimEnabled">false</item>
    </style>

    <style name="AppThemeB" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <!-- specify enter and exit transitions -->
    </style>


</resources>
